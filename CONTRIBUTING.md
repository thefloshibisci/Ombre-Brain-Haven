# 给 Ombre Brain 贡献代码

先说谢谢。这个项目是一群人一点点堆出来的，你愿意花时间读到这里，本身就已经是贡献了。

---

## 项目协议

Ombre Brain 按 [MIT](LICENSE) 发布。你提交的代码会以同样的协议发布出去。

MIT 很宽松：任何人都能用、改、分发、商用甚至闭源，只要保留版权声明。
这意味着你的代码可能会出现在你意想不到的地方 —— 这是选 MIT 时就接受的事。

另有一份 [NOTICE.md](NOTICE.md) 写了关于署名的请求，但那不是条款，没有约束力。

写过代码的人会记在 [AUTHORS.md](AUTHORS.md) 里。名字漏了就说一声，我们补上。

---

## 提交前请签署 DCO

我们用 [DCO](DCO)（Developer Certificate of Origin，开发者原创声明），
Linux 内核、Docker、GitLab 用的是同一套。

### 这是在声明什么

不是把著作权交出来 —— **你写的代码依然是你的**，你可以随便拿去用在别的地方。

签署 DCO 只是说明一件事：**这段代码你有权提交**。
要么是你自己写的，要么来源清楚、协议允许你这样用。

为什么需要这个：如果有一天有人问「这行代码是哪来的、能不能这么用」，
项目得答得上来。没有这层声明，一段来路不明的代码就足以让整个项目变得不能用 ——
不是因为它写得不好，而是因为没人敢担保它的来源。这个风险最后会落在所有使用者身上。

### 怎么签

在 commit 时加 `-s`：

```bash
git commit -s -m "修复 xxx"
```

Git 会自动在提交信息末尾追加一行：

```
Signed-off-by: 你的名字 <your@email.com>
```

这一行就是签署。用的是 `git config user.name` 和 `user.email` 里的值，
**请填写你真实使用的名字和有效邮箱** —— 匿名或者假邮箱的声明没有意义，
将来真要追溯来源时找不到人。用常用 ID 而非法定姓名是可以的，能对应到你本人就行。

### 忘了签怎么办

最后一个提交漏了：

```bash
git commit --amend -s --no-edit
```

一整串提交都漏了（把 `main` 换成你的分支起点）：

```bash
git rebase --signoff main
```

两种都会改写提交历史，如果已经推上去了，需要 `git push --force-with-lease`。
**在自己的分支上做这个是安全的**，不要在别人也在用的分支上改写历史。

---

## 提交之前

跑一下测试：

```bash
python -m pytest -q
```

仓库里本来就有几个已知的红项（备份归档相关），跑之前先看一眼它们在你改动之前是不是就红着。
**判断是不是自己弄红的，看报错文件在不在你的 `git status` 里。**

---

## 关于记忆数据

这个项目处理的是**真实的个人记忆**。

- 不要把任何真实记忆内容放进 issue、PR、测试数据或截图里
- 测试请用独立的空 vault，不要指向自己在用的记忆库
- API Key、Dashboard 密码、token 不要出现在代码、注释、提交信息或对话里

这些不是流程规定，是因为一旦泄漏，损失落在具体某个人身上，而且找不回来。

---

## 有问题就问

不确定该不该改、改在哪里、算不算越界 —— **先开个 issue 问一句**，
比写完一大堆再发现方向不对要省事得多。

问得多不是麻烦，是省了大家的时间。
