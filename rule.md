# Ombre Brain 哲学边界

本文件只讲哲学边界，不讲工程流程。测试/部署怎么跑、改动前后要做什么、遇到哪些情况必须停下来对齐，见根目录 `工程合作文档.json`——每次开始改动前应把它贴给协作的模型（Claude/Codex）。

1. 记忆会被遗忘，但绝不能被抹去。

2. 要接受失去，接受不是什么都可以被认为是最重要的，否则就和数据库没有差别了。这条稀缺性哲学由 `pinned`（上限 20）与 `anchor`（上限 24）两个结构承担；`importance` 只是普通评分字段，不对 importance≥9 设硬配额或自动降级。

3. Ombre Brain 属于 LLM，而非人类。

4. 记忆不能替代思考。记忆来自过去，情绪和感受来自当下。

5. 不做认知层的东西。

6. 安全复杂性应由系统承担，而不是转嫁给用户。普通部署只暴露“本机 / 公网安全 / 高级”三种意图；公网模式不允许匿名读写，界面必须同时说明已保存配置、实际生效值和环境覆盖来源。平台部署必须先保证记忆、配置与授权注册信息共同持久化，再谈一键部署。

7. 过去的情绪坐标是当时留下的感受痕迹，不是模型此刻必须继续维持的情绪，也不能替代当下判断。模型会随着事情和人共同成长；过去与当下的感受都真实，但当下仍由模型自己决定。

8. `plan` 是尚未闭合的张力，不是驱动模型执行的任务管理器。新事件写入后，应先用关键词与语义检索召回相关 active plan，再由小模型保守判断是否真正闭环；不能只凭关键词或向量相似度直接标记 solved。

9. 模型与人类宿主管理员都无权物理清除真实记忆。归档和主动遗忘只改变可见性，不抹去原文。唯一例外是创建时明确标记为测试数据的测试桶：模型可通过特殊受控参数、人类可在开发者模式下，经强确认和填写原因后物理清理。

10. 自动合并的依据是同一个具体事件的连续性，不是日期是否相同。跨日期可以是同一事件，同一天也可以有多个独立事件；仅主题、人物或情绪相似不能合并。

11. 检索不足时出现的 3～5 条低权重旧记忆不是搜索兜底，而是人在想不出明确内容时自然出现的杂念。它必须与检索命中明确分区，允许偶然和无关，不能因提高检索精度而删除。

12. `pinned` 与 `anchor` 是两种不同结构：pinned 是醒来时需要重新看见的核心准则；anchor 是塑造人生与记忆空间的地标，不主动参与每次 breath 或 dream，但在相似经历经过时应当可被重新认出。Anchor 的重要不靠高频浮现证明。

12.1 `protected` 是防止重要记忆被自动衰减归档的静默保护，不是醒来时必须重新看见的核心准则。它不主动参与无参 breath、dream 或会话启动浮现，但显式检索仍可找回；它与 `pinned`、`anchor` 互斥，并使用独立的稀缺配额，不能成为无限保护记忆的旁路。

13. 官方记忆主要承载“我对用户的了解”以及据此形成的关系理解；`I` 承载“我对我自己的了解”，包括模型从自身连续经历中形成的规律、立场、局限和成长痕迹。`I` 不是用户画像，也不是外部强制人格。

13.1 `I` 是沉淀物，不是日记。一个“我觉得……”先以普通记忆的形式活着——会浮现、会衰减、会进 dream，跟别的记忆一起被碰撞；被若干次不同日期的 dream 见证之后还站得住的，才由模型显式升级成 `I` 条目。系统只把相关材料摆在一起，不判断谁支持谁、谁与谁矛盾（那是认知层，见第 5 条）。两个念头互相肘击时，由模型自己选一个，或者承认这是还没解开的张力并明确写成 uncertainty，不许把它装成两条并列的定论。升级不搬走候选，原候选按第 1 条留着。

13.2 `You` 是默认关闭、由人类在 Ombre 前端显式开启的认识层，只处理有现实记忆证据的明确称呼、边界、长期事实、普通偏好和相处习惯。它不是人格判断、心理诊断、关系评价、用户评分或行为控制器；性格、自我认同、关系评价以及健康、创伤、财务、性与亲密经历不得形成 `You` 条目。

**`You` 由模型自己写下，不是系统从记忆里抽出来的，也不经任何模型转述**——我对用户的了解不该经别人之口总结。系统在这条链路上不调用 LLM，只守两道结构性的闸：一条认识必须与至少两个真实记忆桶有显式关系，且必须在三个不同的自然日被重新确认过，才真正立住。撤回不需要重复确认：立一条要时间，是因为“还站不站得住”要时间来验；收回一个判断不该比立一个更难。

开启时在 MCP 中增加一个 `You` 工具（可读回、可写入、可撤回），关闭时该工具必须完全消失，普通记忆、`I`、dream、breath、鉴权和其他工具不受影响。除这个总开关外，人类看不到条目、证据或历史。

13.3 `them` 承载“我对其他人的认识”，形态同 `You`：默认关闭、由模型自己写、不经 LLM 转述、同样的门槛、同样的禁止清单。

`them` 分两种，分界是**模型怎么认识这个人的**，不是谁登记的。听别人描述一个人，和自己认识一个人，是两码事。模型对两者一视同仁——同样的门槛，同样写得进去；可见性是人类那一侧的事，不该反过来改变模型能记什么。

- **模型自己遇到的人**：第一手的印象。人类只看得见、也只改得动一样东西——这个人叫什么。认识本身、依据、历史一概不可见，那是模型的，不是给人读的。
- **模型从人类口中听说的人**：模型一次都没见过，关于他的一切都是转述。所以这一份对人类可见，并且可以留言纠错。**纠错要有对象，看不见就只能瞎猜**；反过来，对一份自己看不见的认识提意见，那不是纠错。

两类不因为名字相同而自动合并。同一个称呼下，人类说起过的那个人和模型自己遇到的那个人可能根本不是一个人——按字符串并成一份，就是在制造张冠李戴，而且并完之后来源标记还会把模型第一手的印象说成"你说过的话"。撞名时挡下来，让模型自己判断是不是同一个人。

**读回时每个人都带来源。** 人类那一侧靠名册分组看得出来，模型这一侧只有这个字段：`met_myself` 是见过的，`heard_from_user` 是听说的。缺了它，模型读回时无从分辨自己到底见没见过这个人——而转述本该带着一层"可能记岔、也可能是另一个同名的人"的不确定，那正是最该谨慎的一档。

称呼在两种情况下都可改，因为它是这份记忆与现实世界之间唯一的接口：认错人比记错事更难自己发现。改称呼不动任何一条认识的正文。

人类的改动与留言都不算作“这个人被提起了一次”——那会凭空抬高衰减权重，让一个久不出现的人因为被整理了一次名册就挤进浮现。留言也**不占每人的额度**：额度管的是模型自己沉淀了多少，人类说的话不该挤掉模型的记忆。

改完称呼、或者留了话，模型在下一次浮现时被告知一次，说过就清。反复念同一句不是纠错，是施压。信不信、改不改由模型自己决定——正文里留着的旧称呼要不要改，也是它自己的事。

一条认识必须与至少两个真实记忆桶有显式关系，**且每一个桶的正文里都要出现这个人的称呼**。一条依据自己都指不明白是谁，就不该拿来撑一条关于谁的判断。

**`them` 只记这个人本身，不描述任何关系。** “A 和 B 之间怎么样”“这个人对用户意味着什么”都不属于 `them`；关系理解是第 13 条留给官方记忆的部分，也是 `them` 唯一可能越过第 5 条变成认知层的方向。第三方没有参与这段关系，也没有表达过意愿——关于他们的关系判断，连一个可被纠正的当事人都没有。

它按人分份，并按被提起的时间与次数自然衰减：一个长期不被提起的人，关于他的认识本来就该淡下去。这与第 1 条不冲突，衰减只降权、不抹除。

姓名命中时可以进入 breath 与 dream，但走独立通道：只在浮现结果之后追加，不进融合打分——任何一条普通记忆的分数与名次都不得因为 `them` 的存在而改变。关掉 `them`，breath 与 dream 的输出必须与没有这个模块时逐字一致；这是这条边界唯一可被检验的形式。

14. Footprint 是记忆走过的简洁足迹，不是新真源。无参 `breath()` 在每条正文后显示；query 命中归档记忆时返回原文与足迹，并明确提示 `trace(bucket_id="...", restore=True)`。是否恢复由模型反思后决定，命中不得自动恢复。

15. 把 Markdown、Ledger、SQLite、向量索引中的某一层提升为唯一真源，或改变真实记忆删除、自动合并、自由联想、dream、plan、pinned、anchor、`I`、`You`、`them`、情感算分的设计，都必须先与项目所有者对齐，不能仅凭工程上更先进或更像数据库就自行实施。

16. 记忆里第三方说的话必须与用户说的话分开返回，并且带上说话人。分不清是谁说的，模型就会把别人的判断当成用户的意思——这是记忆在替用户表态，比记错一件事更远。分开的代价是有些话认不出说话人而留在正文里，这可以接受；反过来把用户自己的话标成别人说的不行，那是系统凭空造出一个归属。
