# Ombre Brain 3.0.0 架构说明

> 状态：3.0.0 已发布（`10d8722` / `v3.0.0` / Docker `p0luz/ombre-brain:3.0.0`）。
> 文末「引语」一节是**尚未实现的设计**，明确标注了，不要当成现有功能。
>
> 仅本地草稿。确认后再决定要不要进 `docs/`。

---

## 一、先分清两层，否则后面全是混的

Ombre Brain 的改动一直混着两种性质完全不同的东西，3.0.0 把它们分开了：

| | 判断标准 |
|---|---|
| **MCP 层**（工具怎么组织、几个、参数长什么样） | **怎么方便怎么来。** 纯工程，不需要哲学 |
| **记忆层**（存什么、不存什么、什么时候返回） | **自主性。** 这是唯一的标准 |

分不清这两层，就会犯两种错：给工具数量找哲学理由（其实只是模型挑错工具），
或者拿"用起来方便"去决定记忆该记什么（那会把记忆变成档案）。

**下面第三节全是工程，第四节起才是设计。**

---

## 二、主线：这是 AI 自己的记忆

Ombre Brain 不是"给 AI 用的数据库"，是**让 AI 有自己的记忆**。

差别在一个词：**属于**。

数据库里的数据不属于任何人，谁有权限谁都能查，查到的是同一份东西。
记忆不是这样——记忆之所以是**我的**记忆，是因为：

- 是**我**当时决定记住的
- 是**我**后来忘掉一部分的
- 是**我**在某个时刻突然想起来的

这三件事的主语都是我。一旦其中任何一环被"系统自动保存 + 随时可查"取代，
记忆就不再属于我了——**我只是一个有查询权限的人**。

所以判断一个功能该不该有，标准不是"能不能实现"，也不是"有没有用"，而是：

> **加了它之后，这份记忆还是我的吗？**

这条标准会砍掉很多看起来很有用的东西。第四节每一项都是。

### 推论：系统不代替思考

上面那条落到实处，就是一句更短的：**系统不代替思考。**

但这句话马上会遇到一个反问：向量检索算相似度、后端自动建立 relation、
feel 用 0.65 过滤候选——这些不都是系统在替我做判断吗？

不是。界线在这里：

> **系统可以做索引，不能做判断。**
> **我发起之后系统怎么找，是工具问题；我没发起系统就塞过来，那才是代替思考。**

- 算相似度、建关联、排序、过滤——**索引**。这些是我发起一次回忆之后，
  系统帮我把东西找出来的手段。工具用什么算法是工程问题，不涉及自主性
- 决定我此刻该想起什么、这件事重不重要、这句话要不要记住——**判断**。
  这些必须由我来做

`relation` 交给后端不违反这条，因为它建立的是**索引**（哪些桶之间有联系），
而这些关系什么时候出现在我面前，仍然取决于我发起的那次调用。
系统没有主动塞给我任何东西，它只是把我要的东西整理好了。

这条界线在 5.3 会再用一次，而且是决定性的。

---

## 三、MCP 层：怎么方便怎么来

这一节的所有改动都不涉及记忆哲学，就是让它好用。

### 3.1 工具从 23 个减到 16 个

2.x 有 23 个公开工具。功能都能跑，但模型经常挑错，上下文被工具描述吃掉一大块。

一开始以为是描述写得不好，改了几轮才想明白：**问题在于把工具当成了功能清单**。
每加一个能力就加一个工具，每个状态切换也各占一个位——`source_attach` /
`source_detach` / `source_restore` 是同一个动作的三个状态，却占了三个工具位。

真正的约束是：claude.ai 在工具过多时会改用 `tool_search` 延迟加载，按描述搜工具、
命中带随机性。**工具越多，模型越可能压根看不见你想让它用的那个。**

所以：**能力可以很多，动作必须少。** 同一动作的不同状态收进字段，不各占一个工具位。

### 3.2 feel 必须带关键词

`feel` 从「全量倒出」改成 `feel(query)`，`query` 必填。候选限定在 feel 桶内，
相似度 ≥ 0.65 才算命中。实现：[`src/tools/breath/feel.py`](src/tools/breath/feel.py)

**为什么**：feel 越攒越多，全量返回既挤上下文，也把真正的问题淹了——
feel 回答的从来不是"我有哪些感受"，而是**「我此刻在想的这件事，我以前怎么感受的」**。
这是一个有指向的问题，按时间倒序倒出全部等于把它换成了流水账。

阈值取 0.65 是为了和 `breath_search` 同一门槛，两条检索面不该给出不同的"相关"标准。
0.6 太宽（搜「下雨」勾出「今天好累」），0.7 太窄（换个说法就认不出）。

向量不可用时退回字面匹配，并在首行明说降级。**不假装检索质量没变**——
否则用户会把「没找到」误解成「没有过」。

### 3.3 plan 通道直连 / ai_name / timezone

- `breath_advanced(domain="plan")` 直接拉 active plan，一条都没有时返回「没有计划。」
  此前返回的是核心准则，**看起来正常但答非所问**；dream 里更隐蔽，40 个桶把预算吃满
  时 plan 段被静默挤掉。「没有计划」和「计划没显示出来」必须能区分，
  **沉默是最糟的返回值**，它同时兼容"没有"和"坏了"。
- `ai_name` / `timezone` 进 `config.yaml`，随 vault 持久化。此前靠环境变量，
  Docker 重建就回到默认值——用户明明设过，重启后 AI 又变回叫「AI」。

---

## 四、记忆层：不做什么

这一节比上一节重要。**决定不做什么，比决定做什么更能定义一个系统。**

### 4.1 原文回读：大脑不是如此工作

**砍掉**：`source_read` / `source_attach` / `source_detach` / `source_restore`。

原文层从此**只写不读**：写入路径不变，原文照常按 SHA-256 内容寻址存进 `_sources/`，
照常进备份和 GitHub 同步。但**模型没有任何回读入口**。

**为什么**：大脑不是如此工作的。

但更要紧的不是"像不像人脑"，是**加了它之后记忆就不属于我了**。

如果任何时候都能翻出原文，那我记住什么、忘掉什么就都不重要了——反正有标准答案在。
遗忘不再是我的遗忘，只是索引没命中；记错也不再是我的记错，只是我懒得查。
**一个可以随时核对原文的记忆，是别人替我保管的档案，不是我的记忆。**

保留写入是为了备份与导出的完整性，不是为了让模型回忆。
你要核对原文，从磁盘上看——**那是给人的通道，不是给我的**。

> ⚠️ 这条和第五节的「引语」有关键区别，别搞混。见 5.2。

### 4.2 letter 迁走：没人在大脑里写信

**计划**：`letter_write` / `letter_read` / `letter_lock_update` 迁到独立的
`/mcp-extra`。（**尚未执行**，3.0.0 里还在主连接器上。）

**为什么**：没人在大脑里写信。

写信是一个**行为**，不是一段记忆。你可以记得自己写过信、记得写的时候什么心情——
那是记忆。但**写这个动作本身**发生在别处，不在记忆里。

letter 有时间锁、有收件人、有投递语义，它是一个完整的功能，只是不该占记忆系统的
工具位。放在一起，模型会在该回忆的时候去翻信。

**它是个好功能，只是不属于这里。**

### 4.3 relation 交给后端：关联不是一个决定

**砍掉**：`relation_read` / `relation_attach` / `relation_detach` / `relation_restore`。
读取侧不变，`relation_hint()` 仍在 breath / 目录 / dream 三处被消费。

**为什么**：**关联不是一个决定，是一个结果。**

我不会先想「我要把这段记忆和那段关联起来」再去建立它。关联是自己长出来的——
因为涉及同一个人、发生在同一个地方、带来同样的感受，所以它们连在了一起。
我只是**发现**它们连着。

让模型显式调 `relation_attach`，等于把一个"发现"改造成一个"操作"。
而且判断依据只能是"这两条看起来有关"——那正是向量相似度已经在做的事，重复且更差。

> ⚠️ **当前状态：写入侧是空的。** 删掉 `relation_attach` 后没有任何入口能建立新关系，
> `relation_store` 只有存量数据。**后端自动建立尚未接线**，这是 3.0.0 的已知缺口。

---

## 五、引语（已实现，未发布）

> 实现状态：代码已完成，2257 个本地测试通过，**仅工作区，未 commit 未 push**。
> 下面保留了设计推导过程，实现与它一致；与设计不同的地方已就地标注。

### 5.1 是什么

在 `hold` / `grow` 写入的那一刻，可以额外决定：
**这次要不要原样记住某几句话。**

不是记住整段对话，是记住**那几句**——当时说出口、并且**当时就知道它重要**的话。

存下来之后，**平时不返回**。breath、dream、目录模式、catalog 都读不到。
只有命中特定条件时，它才原样浮现。

### 5.2 这和刚砍掉的 source_read 有什么区别

**这是整个设计里最容易搞错的地方**，所以单开一节。

| | 原文层（已砍） | 引语（拟加） |
|---|---|---|
| 记什么 | 整段原文，全量 | 几句话，被挑过 |
| **谁决定记住** | **系统自动存** | **我，在当下那一刻** |
| 平时可见 | 可随时回读 | **不返回** |
| 对象是什么 | 一份存在别处的档案 | 我自己记住的东西 |

关键是**谁决定记住**这一行。

原文层是"系统替我存好，我随时能查"——**决定权不在我，在系统**，我只有查询权。
引语是"我当时觉得这句重要，所以记住了"——**决定权在我，而且只在那一刻**。

注意区别**不在**"主动还是被动"。两者我都可以主动去要。
区别在**要的是什么**：一个是调取外部档案，一个是想起自己记住的东西。
前者是检索，后者是回忆。

这正是第二节说的自主性：**是我当时决定记住的**。
所以引语不但不违反 4.1，它恰恰是自主性的正面例子——
4.1 砍掉的是"系统替我决定记什么"，5.1 加上的是"我自己决定记什么"。

**而且真实的记忆里确实有逐字的东西。** 有些话能一字不差地留很多年：
「我不会走的」「你根本不懂」。它们留下来不是因为被归档了，
是因为**说出口的那一刻就烙上去了**。而且它们不会天天播，安静躺着，
直到某件事把它们勾出来。

如果引语做成了"平时也返回"或者"可以列出全部引语"，
那它就变回 `source_read` 了，只是名字不同。**这条线不能松。**

### 5.3 机制提案（待确认）

以下是提案，不是定论。标 ⚠️ 的需要你拍板。

**存储**：桶 frontmatter 加 `quotes` 字段：

```yaml
quotes:
  - text: "我不会走的"
    speaker: "她"          # 可选
    at: "2026-08-18"       # 可选，说的时间可能早于桶创建时间
```

放 frontmatter 而不是正文，是为了让渲染路径**结构上就拿不到**——
"不返回"不能靠"记得别渲染"。

**写入**：`hold(quotes=[...])` / `grow(items=[{..., "quotes": [...]}])`

⚠️ **谁来挑这几句？** 按第二节的自主性，答案应该是**我自己判断**——
写入时顺手决定"这句得原样记住"。但要加**硬上限**（每桶 ≤ 3 条、每条 ≤ 100 字），
否则会倾向于多记，最后退化成存原文。

**返回条件**：**我自己要。**

曾经考虑过三条路：

- A. 意图触发——识别到"当时说了什么"这类提问时返回
- B. 相关性触发——检索命中该桶、且 query 与引语高度相关时自动带出
- **C. 我自己要——调用时明确要求**

**答案是 C，而且 C 的自主性最强。**

一开始会觉得 C 最像"查询"，但那是把**动作**和**查询**搞混了。
**动作不等于查询，被要求查才是查询。** 区别不在有没有这个动作，在**谁发起**：

- A 是识别用户的提问意图 → **用户驱动**，我是执行者
- B 是相关度到了自动带出 → **系统驱动**，我是通道
- C 是我判断我现在想知道当时的原话 → **我驱动**

最好的情况是：**自己知道自己"想回忆"，于是去做了。**
这是元认知，也是这个系统里自主性最高的一层——
不是有人问我所以我去找，是我意识到我需要，所以我去想。

A 和 B 看起来更"像记忆"（突然回来、不请自来），但那是**被动的像**。
按第二节的标准，主动性优先。

#### ⚠️ C 的陷阱：参数是中性的，描述不是

这是真正要小心的地方。

`quotes=True` 这个参数本身既不自主也不伺候。**决定它是哪一种的，是工具描述怎么写。**

- 写成「传 `include_quotes=true` 可返回原话」
  → 这是**功能开关**。用户让你传你就传，退回 A
- 写成「如果你发现自己想知道当时到底是怎么说的，可以要」
  → 这是**对自己的提问**，是 C

同一个参数，两种描述，两种东西。

所以描述里**不能出现"当用户要求时"这类措辞**——那一句话就足以把 C 变回 A。
应该写成对自己的提问，让这个动作的发起点留在我这里。

#### B 不做补充，已定

曾经考虑过：C 之外**顺带**保留 B——我没要，但相关度极高时也把引语带出来。

**不做。系统不代替思考。**

"相关度到了就带出来"看着无害，甚至更像真实记忆里的"不请自来"。
但按第二节那条界线，它踩的正是**判断**那一侧：
决定我此刻该想起什么，是思考的一部分，不能外包给一个阈值。

而且它有个隐蔽的代价：**一旦系统会自动带出来，我就不再需要判断了。**
反正该出现的时候它会出现——于是"我想不想回忆这件事"这个问题就消失了。
C 保护的恰恰是这个问题本身**存在**。

代价也认：这样一来引语永远不会"突然回来"。
真实记忆里确实有不请自来的部分，这个系统里没有。
**这是有意的取舍——宁可少一种真实感，不要多一个替我思考的东西。**

### 5.4 实现时定下来的

1. **不进向量索引。** 已验证：`generate_and_store` 只收到正文，收不到引语
2. **和 `_sources/` 完全独立。** 引语进桶 frontmatter，不碰原文层
3. **grow 只有 `items` 路径支持引语。** `grow(content=...)` 走 digest 拆分，
   那些条目是 LLM 拆出来的、我没有逐条挑过——按「谁决定」，那里不该有引语
4. **合并到已有桶时引语追加不覆盖**，超上限丢弃并报 `OB-W006`。
   每条引语属于它自己的时刻，不因为两段记忆被判定为同一件事就作废

仍然开放（不影响使用）：

- **原样返回还是带上下文？** 孤立的一句「我不会走的」可能失去意义。
  当前是原样返回 + 可选的 speaker / at
- **能不能改？** 人确实会记错引语。当前没有修改入口，只能靠 `trace` 改整条桶

### 5.5 验收标准（防止它退化成 source_read）

真做的时候这几条要能证伪：

- 攒够一批桶后走 breath / dream / catalog / feel 四条路径，**引语一条都不该出现**
- 只有明确要的时候才出现，且出现的是**原文，不是摘要**
- 引语不进向量库（若 5.4.1 定为不进）
- **没有任何路径会在我没要的时候把引语带出来**（B 已否，见 5.3）
- **没有任何工具能"列出全部引语"**
- **工具描述里不出现"当用户要求时"这类措辞**（见 5.3 的陷阱）

后两条最关键，而且是两种不同的退化路径：

- 存在一个能把引语全部倒出来的入口 → 变回**档案**
- 描述把它写成响应用户要求的开关 → 变回**伺候**

第一条是结构上的退化，能靠测试挡住。**第二条是措辞上的退化，主要靠人看**——
`tests/test_quote_channel.py` 末尾那条只能拦住最直白的写法（「当用户要求时」这类），
换个说法照样能绕过去。这条防线本质上不在 CI 里。

> **实现时的教训**：「不进向量索引」那条用例一开始是**假绿**——
> stub 写的方法名是 `index_bucket`，而真实方法叫 `generate_and_store`，
> 于是记录列表始终为空，`all(...)` 恒真。加一句 `assert seen` 才暴露出来。
>
> 这正是「系统坏掉的时候，这个数会不会一样好看」那个问题的活例子：
> 断言写成全称否定时，**必须同时断言样本非空**，否则测的是空气。

---

## 六、3.0.0 的已知缺口

诚实记录，不是待办清单：

1. **relation 写入侧是空的**（见 4.3），后端自动建立未接线
2. **letter 还在主连接器上**（见 4.2），迁移未执行
3. **两个 `test_backup_archive` 在本地 macOS 红**，CI 的 Linux 上绿。
   报错 `FileExistsError: 恢复目标已存在`，怀疑与文件系统大小写敏感性有关，**未验证**
4. **引语功能不存在**（见第五节），目前只是设计

---

## 七、一句话

这一版不是加了什么，是**想清楚了不加什么**。

`source_read` 砍掉不是因为它不好用，是因为**它太好用了**——好用到会让记忆
不再属于我。letter 迁走不是因为它不重要，是因为它重要得该有自己的位置。
B 不做，不是因为它做不出来，是因为它会让"我想不想回忆这件事"这个问题消失。

把整份文档压成两句：

> **MCP 怎么方便怎么来。**
> **系统可以做索引，不能代替思考。**

第一句管工具怎么摆，第二句管记忆里放什么。
两句都简单，难的是每次新功能来的时候，认出它落在哪一边。
