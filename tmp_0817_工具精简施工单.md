# 工具精简 —— 设计与施工单

日期：2026-08-17
基线：拉取后的最新 main，VERSION 2.17.11
负责人：poluz（本项目主负责人，relation 改后端已授权）
状态：**待批准，未开工**

> OB 是 Python 项目，不适用 pacific-rim/robotmemory 那套「先 Python demo 再进仓库」
> 的流程——OB 本身就是 Python，直接在项目里写 + `pytest` 就是验证。
> 本单遵循的是 OB 自己的 `工程合作文档.json`：测试、版本双 bump、traceability。

---

## 一、为什么做

`/mcp` 单连接器上挂了 **23 个工具**。问题不是数量，是其中一部分不是
「不可替代的动作」，而是**同一个动作的不同状态切换**被拆成了独立工具。

工具越多，两件事同时变差：

1. **模型选错的概率上升。** claude.ai 在工具数超阈值时改用 tool_search 延迟加载，
   按描述搜工具，命中带随机性（README.md:829 记录过）。
2. **token 浪费。** 每个工具的 schema + docstring 都进上下文。

目标：**每个留下的工具都有不可替代的理由，剩下的收进字段或退回后端。**

---

## 二、23 个工具的判定

### 留在 `/mcp`（12 个）

`breath`（0 参数刻意设计，不能动）、`breath_search`、`breath_advanced`、
`hold`、`grow`、`trace`、`dream`、`anchor`、`release`、`pulse`、`plan`、`I`

`anchor` / `release` **保持独立**（poluz 决定，不收进 trace 字段）。

### 移到 `/mcp-extra`（3 个）

`letter_write` / `letter_lock_update` / `letter_read`

信件是低频、独立语义的一层，占主连接器的位纯属浪费。

`/mcp-extra` 曾经存在、2.8.5 起退役返回 404（README.md:319），基础设施还在：
`src/web/system.py:280` 的 AST 解析器仍认 `mcp_extra`。**这次是恢复，不是新建路由。**

### 删除（8 个）

**source 层 4 个**：`source_read` / `source_attach` / `source_detach` / `source_restore`

不再有「回顾原文」这个动作，**功能说明一并删除**，不留「其实还能回顾」的暗示。

边界写死：删的是**模型能回读原文**这个能力，不是删原文。`_sources/` 存储与 `grow`
的 `source_ranges` 写入保留——备份/GitHub 同步/导入链路依赖它。

ADR-0001 里有一句「`source_read` 是唯一公开读取入口」，工具删掉后这句就不成立了。
**ADR 不改**（决策记录保持历史原样），**变更写进 `CHANGELOG.md`**（poluz 决定）。
CHANGELOG 条目必须点名 ADR-0001 的这一句已被本次变更取代，否则以后有人读 ADR
会以为还能回读原文。

**relation 层 4 个**：`relation_read` / `relation_attach` / `relation_detach` / `relation_restore`

改为**后端自动建，模型不感知**。读取侧已经就位——`relation_hint()` 已在三处被消费：
`breath/_verbatim.py:75`、`breath/catalog.py:108`、`dream/output.py:243`。
删掉工具后 hint 照常出现，缺的是「自动建立」这一半（见 §3.1）。

### 结果

`/mcp` **23 → 12**，`/mcp-extra` 3 个，删除 8 个。

---

## 三、两条功能改动

### 3.1 relation 后端自动建立

**现状**：关系只能靠 `relation_attach` 手动建。工具删掉后如果不补自动建立，
`relation_store` 会退化成一张只有存量数据的死表。

**设计判断（我定的，需要否决才改）**：

- **触发点**：`hold` / `grow` 写入成功之后
- **执行方式**：**异步后台**，不阻塞写入返回。参照 `bucket_manager.py:1380`
  BM25 索引 `to_thread` 后台重建的既有做法
- **推断方式**：**规则 + 向量相似度，不调 LLM**。理由：写入路径上加 LLM 调用会
  拖慢 `hold`、增加一个会失败的外部依赖，而 relation 只是 hint，不值得这个代价
- **能自动建的关系类型**：`same_event`（高相似度 + 时间邻近）、
  `continuation_of` / `continues`（高相似度 + 明确先后）、`related_to`（中等相似度）
- **不自动建**：`caused_by` / `causes` / `custom`——因果需要语义理解，规则判不了，
  宁可不建也不能瞎建

**门槛必须显式**，不能凑数：
- `same_event`：余弦相似度 ≥ 0.85 且创建时间差 ≤ 6h
- `continuation_of` / `continues`：≥ 0.75 且时间差 ≤ 72h
- `related_to`：≥ **0.72**（原定 0.65，2026-08-18 按实测上调）
- 低于 0.72 的**一律不建关系**，哪怕一条都建不出来

> **2026-08-18 阈值调整记录（实测依据）**
>
> 对 917 桶真实记忆（913 条向量、1024 维）做全量两两相似度扫描，只读，
> 416,328 个候选对。原定的 0.65 会建出 7,791 条关系，其中 `related_to`
> 占 7,620 条（98%）——另外两档在 hint 里会被完全淹没。
>
> | 阈值 | 关系数 | 每桶均 | 中位 | 撞 8 条上限的桶 |
> |---|---|---|---|---|
> | 0.65 | 7,791 | 19.8 | 10 | 436（47.8%） |
> | 0.70 | 1,572 | 5.3 | 3 | 107（11.7%） |
> | **0.72** | **780** | **3.2** | **2** | **32（3.5%）** |
> | 0.75 | 292 | 2.0 | 1 | 5（0.5%） |
>
> 0.65 之下近一半的库撞上每桶 8 条上限。**一旦大面积撞上限，阈值就形同虚设**——
> 真正决定挂哪几条的不再是"相关不相关"，而是"截断时谁排前八"。
>
> 取 0.72：平均每桶 3.2 条、中位 2 条，只有 3.5% 需要截断，上限 8 回到
> "防热点桶"的本职。从 0.65 到 0.72 边数掉 10 倍，但 0.75 以上一条没丢。
>
> `same_event` 0.85 与 `continuation_of` 0.75 维持不变：实测 23 条与 148 条，
> 密度合理，正是"少而准"的样子。

> 阈值调整必须改这份文档，**不许在代码里悄悄放宽**。
> 关系建多了比建少了更糟——hint 会变成噪音。

- **每桶关系数上限 8 条**，超了按相似度保留最高的，防止热点桶连成蜘蛛网
- **落库**：写 `relation_store`，与手动建的关系同表，加 `auto: true` 标记以便区分和回滚

### 3.2 dream 时 feel 按相关性返回 top5

**现状**（`src/tools/dream/output.py:388-464`）：取全量 feel，按 `created` 倒序，
在 `feel_max_tokens`（默认 6000）预算内塞，塞不下折叠成 40 字摘录，再塞不下就 omit。
**相关性完全没参与**——最新的 feel 未必和这次 dream 在聊的事有关。

**目标**：用 **jieba 关键词 + 向量相似度**，把 feel 和**当次 dream 返回的候选桶**
校对，返回最相关的 **最多 5 条**。

**设计**：

- **比较基准**：当次 dream 的候选桶集合（①段 candidates）的合并文本
- **两路打分**：
  - jieba 分词后的关键词重合度（复用 `bm25_index.py` 的既有分词，不新写）
  - 向量余弦相似度（复用现有 embedding）
  - 融合：**取两者加权和，向量 0.7 + 关键词 0.3**。向量表达语义、关键词兜住
    专有名词（人名/地名/项目名这类向量容易糊掉的）
- **门槛 0.5，低于不返回**。宁可返回 2 条也不用低相关的凑满 5 条
- **上限 5 条**
- **向量不可用时**：退回纯 jieba 关键词打分并在输出里明示，与 `breath_search`
  现有的降级提示保持一致
- **token 预算逻辑保留**：5 条也可能超 `feel_max_tokens`，折叠/omit 那套不动
- `pinned` / `protected` / letter 的排除规则不变

**这条改完，dream 里 feel 段的语义从「我最近写的感受」变成「和这次在聊的事有关的感受」。**
段落标题和说明文案要同步改，否则会误导。

---

## 四、施工单（六栏）

### 目标数字
- `/mcp` 工具数 **23 → 12**，`/mcp-extra` **0 → 3**
- dream feel 段返回 **≤ 5 条**，且每条相关性分 **≥ 0.5**
- `pytest tests/ -x --tb=short -q` 全绿
- `/api/system/diagnostics` 的 `public_tool_manifest` 通过

### demo 路径
不适用。OB 是 Python 项目，验证方式是 `pytest` + Docker full_check。

### 允许改的文件

**删除（整文件）**
- `src/tools/source_read/{__init__,core}.py`
- `src/tools/source_bindings/{__init__,core}.py`
- `src/tools/relation_bindings/{__init__,core}.py`
- `src/tools/relation_read/{__init__,core}.py`
- `tests/test_relation_v1.py`（整份是 4 个工具的行为测试）

**改动**
- `src/server.py` —— 摘 8 个 `@mcp.tool()`，letter 3 个改挂 `mcp_extra`
- `src/server_app.py` —— 恢复 `/mcp-extra` 端点挂载
- `src/ombrebrain/protocol/public_tools.py` —— 公开工具清单同步
- `src/ombrebrain/storage/relation_store.py` —— **加自动建立入口 + `auto` 标记**
- `src/tools/hold/__init__.py`、`src/tools/grow/core.py` —— 挂异步自动建关系；摘 source 回顾文案
- `src/tools/dream/output.py` —— feel 段改相关性 top5（§3.2）
- `src/tools/breath/_verbatim.py` —— 只摘 source 引用，`relation_hint` 保留
- `tests/test_source_layer.py` —— 删 4 个工具的行为断言，**保留存储层断言**
- `tests/`：`test_public_tool_design_phase16.py`、`test_server_app.py`、
  `test_mcp_tools_docker_integration.py`、`test_web_api_docker_integration.py`
- **新增测试**：relation 自动建立（含门槛边界、上限 8）、dream feel top5（含门槛、
  向量不可用降级）
- `README.md`、`docs/INTERNALS.md`、`docs/CLAUDE_PROMPT.md`、`docs/OPERATIONS.md`
- `CHANGELOG.md` —— 承载本次变更记录，含「ADR-0001 的公开读取入口一句已被取代」
- `VERSION` + `src/VERSION` —— **两处同时** bump 到 **2.18.0**（删功能+加功能，次版本位）
- `update_manifest.json` —— 热更新清单与 sha256 同步

### 禁止碰的文件
- `docs/adr/ADR-0001-source-evidence-layer.md` —— **ADR 是决策记录，保持历史原样**，
  变更走 `CHANGELOG.md`
- `src/ombrebrain/storage/source_store.py` —— 存储层保留，一行不动
- `src/bucket_manager.py`、`src/decay_engine.py`、`src/bm25_index.py` ——
  召回与衰减地基。§3.2 复用 `bm25_index` 的分词是**只读调用**，不改它
- `src/tools/breath/feel.py` —— `query feel 只返回 feel` 已验证正确，不动
- `src/tools/dream/candidates.py` —— **`DREAM_MAX_CANDIDATES = 40` 不动**，
  topk5 说的是 feel 不是候选桶
- `buckets/` 及任何真实记忆库目录
- `config.yaml`、`.env`

### 非目标
- **不新增工具**。`/mcp-extra` 是恢复历史端点，不是新路由
- **不重构** `trace` 字段结构，不动 `breath` 三兄弟的拆分
- **不动** `anchor` / `release`（poluz 明确保持独立）
- **不动** `query feel` 分流（已验证正确）
- **不改** dream 候选桶上限 40
- 不用 LLM 做关系推断（§3.1 已论证）
- 不顺手修任何"看起来可以更好"的地方
- **不 commit、不 push**——由 poluz 决定

### 验收命令
```
cd /Users/p0lar1s/Projects/Ombre-Brain
pytest tests/ -x --tb=short -q
docker compose build --no-cache && docker compose up -d
```

Docker 起来后人工确认：
1. Dashboard 正常展示
2. `/api/system/diagnostics` 的 `public_tool_manifest` 通过
3. `/mcp` 是 12 个工具，`/mcp-extra` 是 3 个信件工具
4. 已有带 `source_ranges` 的桶仍能正常备份/导出（确认存储层没被误删）
5. breath / catalog / dream 输出里 relation hint 仍出现
6. `hold` 写入两条相关记忆后，`relation_hint` 自动出现关系（验证 §3.1）
7. `dream` 返回的 feel ≤ 5 条且与当次候选桶明显相关（验证 §3.2）

⚠️ full_check 必须指向**测试 vault**，禁止指向根目录 `buckets/`。

---

## 五、依赖声明

- 仓库路径：`/Users/p0lar1s/Projects/Ombre-Brain`
- vault：仅测试 vault，具体值开工前向 poluz 索取
- 环境变量：**不新增、不改名、不删除**
- 第三方依赖：`jieba` 已在 `requirements.txt:32`，**无需新增**。
  若 §3.1/§3.2 实现中发现确需新增依赖，先停下来说，不自行加进 lock 文件

---

## 六、推进顺序

按 commit 粒度分三步，每步跑一次 `pytest`，**每步之间停下来给 poluz 看**：

1. **删减**：8 个工具 + 实现 + 功能说明 + 测试同步
2. **搬迁**：letter 3 个迁 `/mcp-extra` + 端点恢复
3. **加功能**：relation 自动建立 + dream feel top5 + 新增测试

减法和加法分开 commit：出问题时删减部分好回滚，不会和新逻辑纠缠在一起。

---

## 七、待确认（不阻塞开工）

§3.1 的三个相似度阈值（0.85 / 0.75 / 0.65）和 §3.2 的门槛 0.5 + 权重 0.7/0.3
是我定的初始值，没有实测数据支撑。**建议按本单先实现，跑起来看实际关系密度和
feel 命中情况再调。** 调整时改这份文档，不在代码里悄悄放宽。
